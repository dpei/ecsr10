% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta.R
\name{.beta_comorbidity_flags}
\alias{.beta_comorbidity_flags}
\title{Run the beta comorbidity pipeline over one block of encounters}
\usage{
.beta_comorbidity_flags(
  patient_data,
  dx_cols,
  drg,
  comfmt,
  drg_screens,
  wildcard_mode = "wildcard"
)
}
\arguments{
\item{patient_data}{Data frame of encounters}

\item{dx_cols}{Secondary diagnosis columns}

\item{drg}{Integer vector of MS-DRGs, one per row, or NULL for no screen}

\item{comfmt}{Compiled \code{(target, pattern)} lookup for the version}

\item{drg_screens}{Named list of \code{(low, high)} matrices, or NULL}

\item{wildcard_mode}{"wildcard" or "regex"}
}
\description{
Internal. Returns only the 31-column CMRB flag matrix, one row per row of
\code{patient_data}. Like \code{.comorbidity_flags()}, every stage is
per-encounter, so a contiguous block yields exactly the rows the whole-dataset
call would have, which is what makes \code{ncores > 1} identical to serial.
}
\keyword{internal}
